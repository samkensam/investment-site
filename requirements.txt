# Django Framework
Django==4.2.27

# Python environment variable management
python-decouple==3.8

# Database driver for PostgreSQL (optional - for production)
# Uncomment if deploying to production with PostgreSQL
# psycopg2-binary==2.9.9

# WSGI HTTP Server for production deployment
# Uncomment if deploying to production
# gunicorn==21.2.0

# Static file serving for production
# Uncomment if deploying to production
# whitenoise==6.6.0
